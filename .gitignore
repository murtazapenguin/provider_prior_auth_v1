# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# Environment files — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prisma
prisma/migrations/dev.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
.venv/
services/ai/.env

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

/app/generated/prisma

# Uploaded clinical documents (local storage adapter)
/data/uploads/

# Large reference-data folders (254M + 142M + 21M). The deployed app and the
# sidecar don't use them — the DB seed runs from a local checkout, which keeps
# them on disk regardless. Use Git LFS or object storage if a fully
# self-contained repo is ever needed.
UHC/
CMS/
ICD-10 – Full Code Set/

# Python test cache + TypeScript incremental build info
.pytest_cache/
tsconfig.tsbuildinfo
